node_modules/
.env
.env.local
npm-debug.log*
.DS_Store
dist/
coverage/
*.log

# Prompt Guard 2 ONNX model files — fetched per-environment via
# `npm run download:prompt-guard` (see scripts/download-prompt-guard.js).
# Total ~280MB; not appropriate for git.
models/
